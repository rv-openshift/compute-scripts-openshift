apiVersion: v1
kind: PersistentVolume
metadata:
  name: "{{ pv_name }}"
spec:
  capacity:
    storage: "{{ pv_size }}"
  accessModes:
  - ReadWriteMany
  nfs:
    path: "{{ nfs_path }}"
    server: "{{ nfs_svr }}"
    readOnly: false
  persistentVolumeReclaimPolicy: Retain
